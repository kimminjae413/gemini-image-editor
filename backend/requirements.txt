# 최소한의 패키지로 시작 (테스트용)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# 이미지 처리 (기본)
Pillow==10.1.0

# 데이터 검증
pydantic==2.5.0

# HTTP 클라이언트
requests==2.31.0

# 클라우드 서비스 (나중에 추가)
# runpod==1.6.2
# boto3==1.34.0
# redis==5.0.1
