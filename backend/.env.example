# ====================================
# AI Hair Style Transfer - 환경 변수 설정
# ====================================

# 이 파일을 복사하여 .env 파일을 만드세요
# cp .env.example .env

# ====================================
# RunPod GPU 서버리스 설정
# ====================================

# RunPod API 키 (https://runpod.io/console에서 생성)
RUNPOD_API_KEY=your_runpod_api_key_here

# RunPod 서버리스 엔드포인트 ID
RUNPOD_ENDPOINT=your_runpod_endpoint_id_here

# ====================================
# AWS S3 이미지 저장소 설정
# ====================================

# AWS 액세스 키 (IAM에서 생성)
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# S3 버킷 정보
AWS_BUCKET_NAME=hair-transfer-storage
AWS_REGION=ap-northeast-2

# ====================================
# Redis 작업 큐 설정
# ====================================

# Redis 연결 URL
# 로컬: redis://localhost:6379
# Redis Cloud: redis://:[password]@[host]:[port]
REDIS_URL=redis://localhost:6379

# ====================================
# 서버 설정
# ====================================

# API 서버 포트
PORT=8000

# 환경 (development, production)
ENVIRONMENT=development

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ====================================
# 보안 설정 (프로덕션용)
# ====================================

# 허용할 CORS 오리진 (콤마로 구분)
# 개발: http://localhost:3000,http://127.0.0.1:8080
# 프로덕션: https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:8080

# API 요청 속도 제한 (분당 요청수)
RATE_LIMIT_PER_MINUTE=10

# 최대 파일 크기 (MB)
MAX_FILE_SIZE_MB=10

# ====================================
# 모니터링 및 분석 (선택사항)
# ====================================

# Sentry 오류 추적
SENTRY_DSN=your_sentry_dsn_here

# Google Analytics
GA_TRACKING_ID=your_ga_tracking_id_here

# ====================================
# 설정 가이드
# ====================================

# 1. RunPod 설정:
#    - https://runpod.io 회원가입
#    - Console > API Keys에서 API 키 생성
#    - Serverless > Endpoints에서 새 엔드포인트 생성
#    - 엔드포인트 ID 복사

# 2. AWS S3 설정:
#    - AWS 콘솔에서 S3 버킷 생성
#    - IAM에서 S3 읽기/쓰기 권한이 있는 사용자 생성
#    - Access Key ID와 Secret Key 복사

# 3. Redis 설정:
#    - 로컬: docker run -d -p 6379:6379 redis:alpine
#    - 클라우드: https://redis.com/try-free/ 무료 계정

# 4. 테스트 확인:
#    - python -c "from dotenv import load_dotenv; load_dotenv(); print('환경변수 로드 성공')"
#    - curl http://localhost:8000/health
